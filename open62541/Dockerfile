# open62541 adapter — Phase 2 (not yet implemented)
#
# This is a placeholder that will be replaced during Phase 2 implementation.
# The multi-stage build will be:
#
#   Stage 1 (builder): debian-based with cmake, gcc, and pinned open62541 source
#   Stage 2 (runtime): minimal debian base with the compiled binary only
#
# Pinned versions (to be set during Phase 2):
#   open62541:  1.3.x
#   base image: debian:bookworm-slim
#   CMake:      >= 3.12

# Placeholder so the Dockerfile is syntactically valid
FROM debian:bookworm-slim AS runtime
RUN echo "open62541 adapter: not yet implemented (Phase 2)" && exit 1
