cmake_minimum_required(VERSION 3.12)
project(opcua-compat-open62541 C)

set(CMAKE_C_STANDARD 11)

# Phase 2: add open62541 as a subdirectory or find_package dependency.
# Phase 2: add src/ targets for server, client, and fixture parser.
# Phase 2: add tests/ target for fixture parser unit tests.

message(STATUS "open62541 adapter: Phase 2 not yet implemented")
